
 export function suma(a, b) {
    return a + b
}

 export function multiplica(a, b) {
    return a * b
}

 export function eleva(a, b) {
    return a ** b
}

function factorial(a) {
    // Factorial de 5: 5 * 4 * 3 * 2 * 1
    let factorial = 1;
    for (let i = 2; i <= a; i++) {
        factorial *= i;
    }
    return factorial;
} 
    export default factorial //Exportar por defecto, SOLO UNO POR ARCHIVO

    export const libro = "Don Quijote de la Mancha"




export let code="IDi23y4343y42j@"